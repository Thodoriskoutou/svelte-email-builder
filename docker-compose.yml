version: "3.7"
services:
  pocketbase:
    image: spectado/pocketbase:latest
    container_name: pocketbase
    ports:
      - "3416:80"
    restart: unless-stopped
    volumes:
      - data:/pb_data
      - public:/pb_public
      - migrations:/pb_migrations

  email-builder:
    build: .
    container_name: email-builder
    depends_on:
      - pocketbase
    ports:
      - "3417:3000"
    restart: unless-stopped
    environment:
      - POCKETBASE_URL=http://pocketbase # replace with an FQDN via an (SSL enabled for oAuth2) reverse proxy
      # - ORIGIN=https://emailbuilder.mydomain.com # replace with the app url

volumes:
  data:
  public:
  migrations: